# Assets Context

This directory stores all static assets used across the repository.

## Guidelines
- Use descriptive file names for images (e.g., `github-daily-logo.png`).
- Keep image sizes optimized for web viewing.
- Organize assets into subdirectories if necessary (e.g., `assets/images/`, `assets/icons/`).